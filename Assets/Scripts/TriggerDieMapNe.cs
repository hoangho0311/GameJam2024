using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerDieMapNe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("cc");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
